{
  "name": "pmanga",
  "version": "1.0.0",
  "description": "Your new bare-bones project includes minimal organization with a single `main.rs` file and a few assets.",
  "main": "index.js",
  "scripts": {
    "dev": "dx serve --addr localhost",
    "css:build": "tailwindcss -i tailwind.css -o assets/tailwind.css",
    "css:watch": "tailwindcss -i tailwind.css -o assets/tailwind.css --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "tailwindcss": "4.1.17"
  }
}
